--- src/html2md/cli.py.orig	2024-05-23 00:00:00.000000000 +0000
+++ src/html2md/cli.py	2024-05-23 00:00:00.000000000 +0000
@@ -62,6 +62,10 @@
             if '/?' in target_url:
                 target_url = target_url.replace('/?', '?')

+            if not _validate_scheme(target_url):
+                print(f"Error: Invalid URL scheme in '{target_url}'. Only HTTPS and file:// are allowed.")
+                return
+
             print(f"Processing URL: {target_url}")

             try:
