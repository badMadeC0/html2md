{
  "name": "html2md-cli",
  "version": "0.11.6",
  "scripts": {
    "lint": "black --check .",
    "format": "black .",
    "test": "pytest",
    "healthcheck": "node scripts/healthcheck.mjs",
    "self:heal": "node scripts/self-heal.mjs"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
