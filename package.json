{
  "name": "html2md-cli",
  "version": "0.11.6",
  "scripts": {
    "postinstall": "pip install -e . pytest ruff",
    "test": "pytest",
    "lint": "ruff check .",
    "format": "ruff format .",
    "healthcheck": "node scripts/healthcheck.mjs",
    "self:heal": "node scripts/self-heal.mjs"
  },
  "devDependencies": {}
}
