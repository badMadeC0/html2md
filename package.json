{
  "name": "html2md-cli",
  "version": "0.11.6",
  "scripts": {
    "postinstall": "pip install -e . pytest ruff",
    "test": "pytest",
    "lint": "ruff check .",
    "format": "ruff format .",
    "healthcheck": "python scripts/healthcheck.py",
    "self:heal": "python scripts/self_heal.py"
  },
  "devDependencies": {}
}
