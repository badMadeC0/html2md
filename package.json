{
  "name": "html2md-monorepo",
  "version": "1.0.0",
  "scripts": {
    "healthcheck": "node scripts/healthcheck.mjs",
    "self:heal": "node scripts/self-heal.mjs",
    "lint": "eslint .",
    "format": "prettier -w .",
    "typecheck": "tsc -b",
    "test": "vitest run"
  }
}
